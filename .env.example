# =================================================================
# Pratejra Landing Page Environment Variables
# =================================================================
# This file contains all environment variables needed for both
# Cloudflare Pages (development) and AWS (production) deployments.
#
# Copy this file to .env.local for local development.
# =================================================================

# =================================================================
# APPLICATION CONFIGURATION
# =================================================================

# Application environment (development/production)
NODE_ENV=development

# Site URLs (used by Next.js and client-side code)
NEXT_PUBLIC_SITE_URL=https://pratejra.build
NEXT_PUBLIC_API_URL=https://api.pratejra.build

# =================================================================
# AUTHENTICATION & SECURITY
# =================================================================

# JWT Secret for token signing (REQUIRED - Generate a secure random string)
# Use: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-key-here

# Development password for local auth (only used in development)
# This is the password for alex@alexandermoir.com in development mode
ALEX_PASSWORD=your-development-password-here

# =================================================================
# DATABASE CONFIGURATION
# =================================================================

# Database URL for Prisma
# Cloudflare Pages (development): SQLite file-based database
# AWS (production): PostgreSQL or other cloud database
DATABASE_URL="file:./dev.db"

# =================================================================
# AWS CONFIGURATION (Production Only)
# =================================================================
# These variables are only needed when deploying to AWS in production

# AWS Region
AWS_REGION=us-east-1

# AWS Cognito User Pool ID (for production authentication)
AWS_COGNITO_USER_POOL_ID=your-cognito-user-pool-id

# AWS Cognito Client ID
AWS_COGNITO_CLIENT_ID=your-cognito-client-id

# AWS Cognito Client Secret
AWS_COGNITO_CLIENT_SECRET=your-cognito-client-secret

# AWS RDS Database URL (PostgreSQL)
AWS_DATABASE_URL=postgresql://username:password@host:port/database

# AWS S3 Bucket for file storage
AWS_S3_BUCKET=your-s3-bucket-name

# AWS S3 Region
AWS_S3_REGION=us-east-1

# =================================================================
# CLOUDFLARE CONFIGURATION (Development/Production)
# =================================================================

# Cloudflare Account ID (for Cloudflare Workers/Pages)
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# Cloudflare API Token (for deployments)
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# Cloudflare Zone ID (for DNS management)
CLOUDFLARE_ZONE_ID=your-cloudflare-zone-id

# =================================================================
# ANALYTICS & MONITORING
# =================================================================

# Google Analytics Measurement ID (optional)
GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking (optional)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# =================================================================
# EMAIL CONFIGURATION (Optional)
# =================================================================

# SMTP Configuration for email sending
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# =================================================================
# DEVELOPMENT ONLY VARIABLES
# =================================================================
# These are only used during local development

# Next.js development port
PORT=3000

# Enable Next.js bundle analyzer
ANALYZE=false

# Enable Docker build
DOCKER_BUILD=false

# =================================================================
# SECURITY & COMPLIANCE
# =================================================================

# Encryption key for sensitive data (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-32-character-encryption-key

# Rate limiting configuration
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# =================================================================
# DEPLOYMENT SPECIFIC NOTES
# =================================================================
#
# Cloudflare Pages Deployment:
# - Set NODE_ENV=development for staging/preview
# - Set NODE_ENV=production for production
# - Database will use SQLite (DATABASE_URL="file:./dev.db")
# - Authentication uses local provider with ALEX_PASSWORD
#
# AWS Production Deployment:
# - Set NODE_ENV=production
# - Use AWS_DATABASE_URL for PostgreSQL
# - Authentication switches to CognitoAuthProvider
# - Requires AWS credentials and Cognito configuration
# - Use AWS S3 for file storage if needed
#
# =================================================================